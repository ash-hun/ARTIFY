services:
  fastapi:
    image : artify_be_p:version_1
    working_dir: /code/app
    volumes:
        - ../:/code/app
    ports:
        - 3006:3006
    restart: always
    command: uvicorn main:app --host 0.0.0.0 --port 3006 --reload